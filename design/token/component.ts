import { systemPalette } from "./palette"

// interface SystemTheme {
//     theme: { theme: string | undefined, systemTheme: string | undefined }
// }

export const COMPONENT = {
  backgroundColor: systemPalette.dark.background.primary,
//   cardLabel: (theme: SystemTheme) =>
//     theme.theme === 'light' ? 'text-black' : theme.theme === 'dark' ? 'text-tape' : theme.systemTheme === 'light' ? 'text-punkpink' : 'text-black'
}