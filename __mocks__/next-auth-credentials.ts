type CredentialsConfig = Record<string, unknown>

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const credentialsProvider = (_config?: CredentialsConfig) => ({})

export default credentialsProvider
